# Cesium ion access token, used only for the optional photorealistic 3D city layer.
# Get one at https://ion.cesium.com/tokens with the "assets:read" scope.
# Without it the 3D CITY toggle is hidden and the graphite vector map is the only basemap.
VITE_CESIUM_ION_TOKEN=
